{
  "name": "goblin-gold-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "T231 — Capacitor (in-process WebView) Android wrapper experiment for Goblin Gold. Parallel to the shipping TWA; bundles gg1/prod offline. NOT the shipping path.",
  "license": "UNLICENSED",
  "scripts": {
    "sync:www": "node scripts/sync-www.mjs",
    "cap": "cap"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
